أ

# Section 5: Debugging Angular Applications

## 🎯 الغرض من هذا القسم (The Goal)

لن نتعلم ميزات جديدة في Angular، بل سنتعلم كيف نصلح المشاكل.

1. **Understanding Error Messages:** كيف تقرأ "الطلاسم" الحمراء التي تظهر في الـ Terminal أو المتصفح وتفهم سبب المشكلة (Root Cause).
    
2. **Logical Errors:** كيف تكتشف الأخطاء التي لا توقف التطبيق (No Crash) لكنها تعطي نتائج حسابية خاطئة (Bugs).
    
3. **Angular DevTools:** التعرف على إضافة المتصفح الرسمية التي تساعدك في فحص هيكلية التطبيق والأداء.
    

---

## 🛠️ إعداد المشروع (Setup)

المدرب قام بتجهيز نسخة من مشروع "حاسبة الاستثمار" (الذي أنهيناه للتو)، ولكن **أضاف إليه بعض الأخطاء عمداً**.

### الخطوات المطلوبة الآن:

1. **تحميل المشروع:** قم بتحميل الملف المرفق مع هذا الدرس (Starting Project).
    
2. **تثبيت الحزم:** افتح الـ Terminal داخل مجلد المشروع ونفذ:
    

    
    ```    Bash
    npm install
    ```
    
3. **التشغيل (والفشل):** حاول تشغيل السيرفر:
    
    
    
    ```    Bash
    npm start
    ```
    

> [!WARNING] توقع الخطأ!
> 
> بمجرد تنفيذ `npm start`، **لن يعمل التطبيق**. ستظهر لك رسالة خطأ فوراً في الـ Terminal.
> 
> **لا تقلق!** هذا جزء من الخطة. مهمتنا الأولى هي قراءة هذا الخطأ وإصلاحه لكي يعمل السيرفر.

---

# Debugging: Type Mismatch Error

## 1. تحليل الخطأ (The Analysis)

عندما تحاول تشغيل التطبيق، ظهرت رسالة خطأ في الـ Terminal. لنقم بتفكيكها:

- **الرسالة:** `Type 'User' is not assignable to type 'string'`.
    
- **المعنى:** أنت تحاول تمرير **كائن كامل** (Object) يحمل بيانات المستخدم، في مكان يتوقع الحصول على **نص فقط** (String).
    
- **المكان:** `app.component.html:13`. (السطر 13 في ملف HTML).
    

## 2. المشكلة في الكود (The Code)

في ملف `app.component.html`، نحن نحاول تمرير المستخدم المختار إلى خاصية `userId`.

**الكود الخطأ (Buggy Code):**



```HTML
<app-user-tasks [userId]="selectedUser" />
```

المكون `app-user-tasks` يقول: "أنا أنتظر `userId` من نوع `string`"، لكننا أعطيناه `selectedUser` وهو `object`.

## 3. الحل (The Fix)

الحل بسيط جداً، نحتاج لاستخراج الـ `id` من الكائن وتمريره.

**الكود الصحيح (Fixed Code):**

اذهب إلى `app.component.html` وعدل السطر كالتالي:



```HTML
<app-user-tasks [userId]="selectedUser.id" />
```

---

## 💡 درس مستفاد (Key Takeaway)

**أدوات المطور (IDE Hints):**

لاحظ أنك لست مضطراً دائماً لتشغيل السيرفر لترى الخطأ. المحرر (VS Code) يضع **خطاً أحمر متعرجاً (Red Squiggly Line)** تحت الكود الخاطئ.

- **القاعدة:** إذا رأيت خطاً أحمر في VS Code، توقف واقرأ الرسالة فوراً قبل التشغيل. غالباً ما يكون الحل في تلميح الأداة (Tooltip).
    

**الخطوة التالية:** الآن بعد إصلاح هذا الخطأ، جرب تشغيل `npm start` مرة أخرى. هل سيعمل التطبيق أم سيظهر خطأ جديد؟ (تلميح: غالباً سيظهر خطأ جديد، وهذا طبيعي في عملية التنقيح!).


>[!Tip]
>End

---

# Debugging Logical Errors: The Missing Task

## 1. السحر: خرائط المصدر (Source Maps)

كيف استطعنا وضع Breakpoint على ملف `TypeScript` داخل المتصفح، بينما المتصفح لا يفهم إلا `JavaScript`؟

السر هو تقنية **Source Maps**. هي ملفات تنشئها Angular CLI تعمل كخريطة تربط الكود المضغوط الذي يعمل في المتصفح بالكود الأصلي الذي كتبته أنت.

## 2. اكتشاف الخطأ (The Diagnosis)

عندما استخدمنا أداة التتبع (Debugger) وتتبعنا دالة `addTask` في الخدمة، اكتشفنا الآتي:

1. البيانات تصل سليمة من المكون (`NewTaskComponent`) إلى الخدمة.
    
2. المشكلة تحدث لحظة إنشاء الكائن الجديد داخل الخدمة.
    
3. **السبب الجذري:** المتغير `userId` يتم استقباله كمعامل (Argument) في الدالة، لكن **لا يتم استخدامه** عند إنشاء المهمة، وبدلاً من ذلك يتم استخدام قيمة فارغة أو غير صحيحة.
    

## 3. الحل (The Fix)

لإصلاح المشكلة، يجب تعديل ملف الخدمة `tasks.service.ts` لاستخدام الـ `userId` القادم من المعاملات.

**src/app/tasks/tasks.service.ts:**



```TypeScript
import { Injectable } from '@angular/core';
import { type NewTaskData } from './task/task.model';

@Injectable({ providedIn: 'root' })
export class TasksService {
  private tasks = [
    // ... tasks array
  ];

  constructor() {
    const tasks = localStorage.getItem('tasks');
    if (tasks) {
      this.tasks = JSON.parse(tasks);
    }
  }

  getUserTasks(userId: string) {
    return this.tasks.filter((task) => task.userId === userId);
  }

  // 👇 هنا كان الخطأ وهنا التصحيح
  addTask(taskData: NewTaskData, userId: string) {
    this.tasks.unshift({
      id: new Date().getTime().toString(),
      userId: userId, // ✅ التصحيح: استخدام المعامل userId بدلاً من إهماله
      title: taskData.title,
      summary: taskData.summary,
      dueDate: taskData.date,
    });
    this.saveTasks();
  }

  removeTask(id: string) {
    this.tasks = this.tasks.filter((task) => task.id !== id);
    this.saveTasks();
  }

  private saveTasks() {
    localStorage.setItem('tasks', JSON.stringify(this.tasks));
  }
}
```

---

## ✅ التحقق (Verification)

1. قم بحفظ الملف.
    
2. (اختياري) قم بإزالة الـ Breakpoint من المتصفح لكي لا يتوقف التنفيذ كل مرة.
    
3. حاول إضافة مهمة جديدة لأي مستخدم.
    
4. **النتيجة:** المهمة ستظهر الآن في القائمة فوراً لأنها ارتبطت بمعرف المستخدم الصحيح (`userId`).
    


>[!Tip]
>End

---


# Angular DevTools: The Ultimate Inspection Tool

## 🎯 الغرض من هذا الدرس (The Goal)

أدوات المتصفح العادية (Chrome DevTools) رائعة للـ HTML/CSS/JS، لكنها لا تفهم "لغة Angular" (مثل المكونات، المدخلات، المخرجات).

إضافة **Angular DevTools** تملأ هذه الفجوة وتسمح لك برؤية التطبيق كما يراه Angular من الداخل.

---

## 🛠️ التثبيت والتشغيل (Installation)

1. **التحميل:** اذهب إلى Chrome Web Store وابحث عن `Angular DevTools` ثم اضغط **Add to Chrome**.
    
2. **الوصول:**
    
    - افتح تطبيق Angular الخاص بك.
        
    - افتح أدوات المطور (F12 أو Right Click -> Inspect).
        
    - ابحث عن تبويب جديد اسمه **Angular** (قد يكون في القائمة الجانبية `>>` إذا كانت الشاشة ضيقة).
        

---

## 🔍 المميزات الرئيسية (Key Features)

بمجرد فتح التبويب، ستجد ثلاث أقسام رئيسية:

### 1. شجرة المكونات (Components Tab) - الأهم حالياً

هذا هو القسم الذي ستقضي فيه 90% من وقتك.

- **Visual Hierarchy:** يعرض لك هيكلية المكونات (شجرة) بدلاً من شجرة DOM المعقدة. ترى `AppComponent` وبداخله `Header` و `UserInput` وهكذا.
    
- **State Inspection:** عند الضغط على أي مكون، تظهر لك لوحة جانبية تحتوي على:
    
    - **Inputs:** القيم المدخلة للمكون.
        
    - **Outputs:** الأحداث التي يطلقها.
        
    - **Properties:** خصائص الكلاس.
        
- **Live Editing:** يمكنك تعديل قيم الـ Inputs مباشرة (مثلاً تغيير `false` إلى `true`) وسيتفاعل التطبيق فوراً دون إعادة تحميل!
    

### 2. محلل الأداء (Profiler Tab)

- يسمح لك بتسجيل (Record) تفاعل معين، ثم يخبرك كم استغرق Angular وقتاً لتحديث الصفحة (Change Detection Cycles).
    
- مفيد جداً لاكتشاف "عنق الزجاجة" (Bottlenecks) في التطبيقات البطيئة.
    

### 3. شجرة الحقن (Injector Tree)

- يعرض لك كيف تتدفق الخدمات (Services) والاعتمادات (Dependencies) داخل التطبيق. مفيد جداً عند التعامل مع خدمات معقدة ومتداخلة.
    

---

## ✅ ملخص القسم (Section Recap)

بهذا نكون قد انتهينا من قسم **Debugging**. لقد تعلمنا:

1. كيف نقرأ **رسائل الخطأ** ونحدد مكانها.
    
2. كيف نستخدم **Source Maps** و **Breakpoints** لاصطياد الأخطاء المنطقية.
    
3. كيف نستخدم **Angular DevTools** لفحص حالة التطبيق وتعديلها حياً.
    


>[!Tip]
>End

---

