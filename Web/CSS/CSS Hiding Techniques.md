```css
.example {
  display: none;
}
```

Element is completely removed from the layout flow and not visible.

---

```css
.example {
  visibility: hidden;
}
```

Element remains in the layout flow, still occupying space, but is invisible.

---

